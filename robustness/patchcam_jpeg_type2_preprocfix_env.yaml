# PatchCamelyon JPEG compression override (Type 2):
# - train: CLEAN
# - val/test: JPEG compression
# Matches EVA baseline preprocessing: Resize -> CenterCrop -> ToDtype(scale=True) -> Normalize
# JPEG applied BEFORE ToDtype/Normalize (pixel space).
#
# Env:
#   JPEG_QUALITY (int)
#   RESIZE_DIM (int, default 224)
#   NORMALIZE_MEAN / NORMALIZE_STD (can be list or stringified list)

data:
  init_args:
    datasets:
      predict:
        # -------- TRAIN (clean) --------
        - class_path: eva.vision.datasets.PatchCamelyon
          init_args: &PRED_TRAIN_CLEAN
            root: ${oc.env:DATA_ROOT}
            split: train
            download: ${oc.env:DOWNLOAD_DATA, false}
            transforms:
              class_path: torchvision.transforms.v2.Compose
              init_args:
                transforms:
                  - class_path: torchvision.transforms.v2.Resize
                    init_args:
                      size: ${oc.env:RESIZE_DIM, 224}
                  - class_path: torchvision.transforms.v2.CenterCrop
                    init_args:
                      size: ${oc.env:RESIZE_DIM, 224}
                  - class_path: torchvision.transforms.v2.ToDtype
                    init_args:
                      dtype: torch.float32
                      scale: true
                  - class_path: torchvision.transforms.v2.Normalize
                    init_args:
                      mean: ${oc.decode:${oc.env:NORMALIZE_MEAN, "[0.485,0.456,0.406]"}}
                      std:  ${oc.decode:${oc.env:NORMALIZE_STD,  "[0.229,0.224,0.225]"}}

        # -------- VAL (jpeg) --------
        - class_path: eva.vision.datasets.PatchCamelyon
          init_args:
            root: ${oc.env:DATA_ROOT}
            split: val
            download: ${oc.env:DOWNLOAD_DATA, false}
            transforms:
              class_path: torchvision.transforms.v2.Compose
              init_args:
                transforms:
                  - class_path: torchvision.transforms.v2.Resize
                    init_args:
                      size: ${oc.env:RESIZE_DIM, 224}
                  - class_path: torchvision.transforms.v2.CenterCrop
                    init_args:
                      size: ${oc.env:RESIZE_DIM, 224}
                  - class_path: torchvision.transforms.v2.JPEG
                    init_args:
                      quality: ${oc.decode:${oc.env:JPEG_QUALITY}}
                  - class_path: torchvision.transforms.v2.ToDtype
                    init_args:
                      dtype: torch.float32
                      scale: true
                  - class_path: torchvision.transforms.v2.Normalize
                    init_args:
                      mean: ${oc.decode:${oc.env:NORMALIZE_MEAN, "[0.485,0.456,0.406]"}}
                      std:  ${oc.decode:${oc.env:NORMALIZE_STD,  "[0.229,0.224,0.225]"}}

        # -------- TEST (jpeg) --------
        - class_path: eva.vision.datasets.PatchCamelyon
          init_args:
            root: ${oc.env:DATA_ROOT}
            split: test
            download: ${oc.env:DOWNLOAD_DATA, false}
            transforms:
              class_path: torchvision.transforms.v2.Compose
              init_args:
                transforms:
                  - class_path: torchvision.transforms.v2.Resize
                    init_args:
                      size: ${oc.env:RESIZE_DIM, 224}
                  - class_path: torchvision.transforms.v2.CenterCrop
                    init_args:
                      size: ${oc.env:RESIZE_DIM, 224}
                  - class_path: torchvision.transforms.v2.JPEG
                    init_args:
                      quality: ${oc.decode:${oc.env:JPEG_QUALITY}}
                  - class_path: torchvision.transforms.v2.ToDtype
                    init_args:
                      dtype: torch.float32
                      scale: true
                  - class_path: torchvision.transforms.v2.Normalize
                    init_args:
                      mean: ${oc.decode:${oc.env:NORMALIZE_MEAN, "[0.485,0.456,0.406]"}}
                      std:  ${oc.decode:${oc.env:NORMALIZE_STD,  "[0.229,0.224,0.225]"}}
